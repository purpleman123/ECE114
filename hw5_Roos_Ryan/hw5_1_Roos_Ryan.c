#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#define RANGE 99
#define GUESS 3
#define SIZE 8

int main(){
   if (int i=0; i <= SIZE) 
    srand((unsigned)time(NULL)); // random number generation between 0-999
        random = rand() % 99 + 1;
} // loop until unique number is chosen, then add number to given spot in 1xi array until i = SIZE


//say miss if not proper input or if not one of 8 numbers, if one of 8 numbers say bingo and congrats
//they have three guesses, if they miss all three then they lose. correct numbers are read out at end
//clear input!