// passing_pointer.c:  passing pointer to a function
#include <stdio.h>


int main()
{
    int salary = 0, bonus = 0;
    printf("Enter the employee current salary:"); 
    scanf("%d", &salary);
    printf("Enter bonus:");
    scanf("%d", &bonus);
    // Q: how to update salary with the salaryhike function 
    salaryhike(    , );  // sarayhike function does not need to return any value.
    printf("Final salary: %d\n", salary);

    return 0;
}

void salaryhike(     ,     )
{
   
}
