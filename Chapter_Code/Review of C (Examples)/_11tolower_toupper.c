// Listing 7: tolower_toupper.c (by Ming-Lun Lee)
// Converting lowercase letters to uppercase letters
#include <stdio.h>
               // What do you need to include?  

int main(void)
{
    char ch;
    while (( ch = getchar()) != '\n'){
        if (      ){  // if lowercase letter,
                   // display uppercase letter
        }
        else              // otherwise,
            putchar(ch);  // display as it is
    }
    putchar(ch); // display the newline
    return 0;
}




