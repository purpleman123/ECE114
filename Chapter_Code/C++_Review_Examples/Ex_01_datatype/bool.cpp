#include <iostream>

int main(int argc, const char** argv) {
    bool is_ready =    ;
    bool is_not_ready =     ;
    int promise =   ;
    std::cout << is_ready << std::endl;
    std::cout << is_not_ready << std::endl;
    std::cout << promise << std::endl;
    return 0;
}