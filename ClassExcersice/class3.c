#include <stdio.h>
int main(){
    // need 2 variables
    // pseudo code is explaining in words what you need the code to accomplish
    int x, y;
    printf("Enter Integer #1: ");
    scanf("%d", &x); // need & address sign
    printf("Enter Integer #2: ");
    scanf("%d", &y);
    printf("The sum of %d and %d is %d.", x, y, x+y);
    return 0;
}
// LETS GO